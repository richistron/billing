const isPassWordValid = (value: string): boolean => value.length > 6
// Boolean(value.match(/[A-Z]/)) &&
// Boolean(value.match(/[a-z]/)) &&
// Boolean(value.match(/\W/)) &&
// Boolean(value.match(/\D/))

export default isPassWordValid
